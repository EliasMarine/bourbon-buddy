require("dotenv").config({path: ".env.local"}); const key = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY; console.log("Key exists:", !!key); if (key) { try { const parts = key.split("."); if (parts.length !== 3) console.log("Invalid JWT format, expecting 3 parts"); else { console.log("Key parts:", parts.length); const payload = JSON.parse(Buffer.from(parts[1], "base64").toString()); console.log("Payload:", JSON.stringify(payload, null, 2)); } } catch (e) { console.error("Error:", e.message); } }
