{
  "name": "apple-signin-secret-generator",
  "version": "1.0.0",
  "description": "Generate client secret for Apple Sign In using JWT",
  "main": "generate-apple-secret.js",
  "scripts": {
    "generate": "node generate-apple-secret.js"
  },
  "dependencies": {
    "dotenv": "^16.4.1",
    "jsonwebtoken": "^9.0.2"
  }
} 