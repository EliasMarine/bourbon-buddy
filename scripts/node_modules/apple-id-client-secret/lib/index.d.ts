export declare type ClientSecretConfig = {
    keyId: string;
    bundleId: string;
    teamId: string;
    privateKey: string;
};
export declare function createClientSecret(config: ClientSecretConfig): string;
